import { InputFormaterDirective } from './input-formater.directive';

describe('InputFormaterDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFormaterDirective();
    expect(directive).toBeTruthy();
  });
});
